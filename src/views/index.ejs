<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Document</title>

        <link
            rel="stylesheet"
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" />
    </head>
    <body>
        <div class="container">
            <div style="height: 20px"></div>

            <div style="background-color: #29b363; height: 180px">
                <div
                    class="container d-flex justify-content-center align-items-center text-white"
                    style="height: 100%">
                    <div>
                        <div class="fs-1">A joke a day keeps the doctor away</div>
                        <div class="d-flex justify-content-center">
                            A joke a day keeps the doctor away
                        </div>
                    </div>
                </div>
            </div>
            <div style="background-color: #fcfcfc; height: 500px">
                <div class="row">
                    <div class="col-2"></div>
                    <div class="col-8" style="text-align: justify; margin-top: 50px">
                        <div style="height: 200px"><%= dataDisplay[0].content %></div>
                        <form action="/" method="get">
                            <div class="row" style="margin-top: 50px">
                                <% if(dataDisplay[0].id === 0){ %>
                                <div class="col-6 float-end">
                                    <button
                                        disabled
                                        class="btn float-end text-white"
                                        style="width: 50%; background: #2c7edb">
                                        This is Funny!
                                    </button>
                                </div>
                                <% } else{ %>
                                <div class="col-6 float-end">
                                    <button
                                        type="submit"
                                        class="btn float-end text-white"
                                        style="width: 50%; background: #2c7edb">
                                        This is Funny!
                                    </button>
                                </div>
                                <% } %> <% if(dataDisplay[0].id === 0){ %>
                                <div class="col-6">
                                    <button
                                        disabled
                                        class="btn text-white"
                                        style="width: 50%; background: #29b363">
                                        This is not funny
                                    </button>
                                </div>
                                <% } else{ %>
                                <div class="col-6">
                                    <button
                                        type="submit"
                                        class="btn text-white"
                                        style="width: 50%; background: #29b363">
                                        This is not funny
                                    </button>
                                </div>
                                <% } %>
                            </div>
                        </form>
                        <form action="/clean" method="post">
                            <button type="submit" formaction="/clean" class="btn btn-danger">
                                Clean Cookie
                            </button>
                        </form>
                    </div>
                    <div class="col-2"></div>
                </div>
            </div>
        </div>
    </body>
</html>
